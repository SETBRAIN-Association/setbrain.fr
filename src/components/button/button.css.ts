import { style } from "@vanilla-extract/css";

export const button = style({
    background: '#050510',
    color: 'white',
    borderRadius: 4,
    padding: '20px 64px',
    border: 'none',
    fontSize: 22,
    cursor: 'none'
});